{% extends "vueMere.html.twig" %}

{% block title %}
	{{ parent() }} - Index
{% endblock %}

{% block body %}
<<<<<<< HEAD
	<h3><a href="{{ path('oc_platform_add') }}">(Ajouter)</a></h3>
    {% for advert in adverts %}
			<div>
				{% if advert.image is not null %}
					<img 
						src="{{ asset(advert.image.webPath) }}"
						alt="{{ advert.image.alt }}"
					/>
				{% endif %}
				<br>Hello {{ advert.author }} : {{ advert.date|date("m/d/Y") }} <a href="{{ path('oc_platform_remove_advert', {'id': advert.id}) }}">(x)</a>
				{% if not advert.categories.empty %}
				<br>Catégoris :
					{% for category in advert.categories %}
					    <br>- {{ category.name }}{% if not loop.last %}; {% endif %}
					{% endfor %}
				{% endif %}
				<h3> <a href="{{ path('oc_platform_edit', {'id': advert.id}) }}">(Modifier)</a></h3>
			</div>
		{% endfor %}
=======
	{% for advert in adverts %}
		{% if advert.image is not null %}
			<div>
				<img src="{{ advert.image.url }}" alt="{{ advert.image.alt }}">
				<br>Hello {{ advert.author }} : {{ advert.date|date("m/d/Y") }}!
				{% if not advert.categories.empty %}
					<br>Catégoris :
					{% for category in advert.categories %}
						<br>- {{ category.name }}{% if not loop.last %}; {% endif %}
					{% endfor %}
				{% endif %}
			</div>
		{% endif %}
	{% endfor %}
>>>>>>> master
{% endblock %}

{% block footer %}
	{{ include("@BayardTestPlatform/Default/footer.html.twig") }}
{% endblock %}